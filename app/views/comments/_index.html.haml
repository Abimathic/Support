.comment

  - question.comments.each do|c|
    .question_comment
      %span= c.comment
      %span.fullname= "-#{c.user.fullname}"
      - if current_user == c.user
        %td= link_to 'Edit', edit_question_comment_path(id: c.id, question_id:question)
        %td= link_to 'Destroy', question_comment_path(id: c.id, question_id:question), method: :delete, data: { confirm: 'Are you sure?' }
